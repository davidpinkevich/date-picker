import { type DefaultTheme } from "styled-components";

const Theme: DefaultTheme = {
  fontSizes: {
    inputSize: "15px",
    dateBlockItemSize: "13px"
  },
  colors: {
    mainDark: "#333333"
  }
};

export { Theme };
