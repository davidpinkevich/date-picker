export interface TypesTheme {
  fontSizes: {
    inputSize: string;
    dateBlockItemSize: string;
  };

  colors: {
    mainDark: string;
    mainGray: string;
    mainRed: string;
    lightRed: string;
    lightGray: string;
    targetBlue: string;
    black: string;
    white: string;
  };
}
